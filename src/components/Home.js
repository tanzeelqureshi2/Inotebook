import React from "react";
import Notes from './Notes';

const Home = () => {
  
  return (
    <div>
      {/* Use Notes Component for fetching notes */}
      <Notes/>
    </div>
  );
};

export default Home;
